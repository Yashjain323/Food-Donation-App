<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yash.fooddonationapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodDonationApplication">
        <activity android:name=".DonorAchievements"></activity>
        <activity android:name=".DonorHomePage" />
        <activity android:name=".reciepientSetup" />
        <activity android:name=".RecipientSignIn" />
        <activity android:name=".DonorSignIn" />
        <activity android:name=".OrganisationSetup" />
        <activity android:name=".ReciepientRegistration" />
        <activity
            android:name=".ForgotPassword"
            android:exported="true" />
        <activity android:name=".DonatorProfile" />
        <activity android:name=".DecidingActivity" />
        <activity android:name=".DonorRegistration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>